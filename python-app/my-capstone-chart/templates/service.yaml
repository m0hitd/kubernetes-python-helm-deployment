apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-capstone-chart.fullname" . }}
  labels:
    {{- include "my-capstone-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-capstone-chart.selectorLabels" . | nindent 4 }}
